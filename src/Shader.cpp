// Minimal Shader placeholder to satisfy build while the real implementation
// is not present. Add the real Shader class implementation in this file
// (and corresponding header) when available.

namespace meshviewer {
    inline void _shader_placeholder() {}
}
